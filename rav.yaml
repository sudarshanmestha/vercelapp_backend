scripts:
 server:
  - cd dapp && python manage.py runserver 8001

 makemigrations:
  - cd dapp && python manage.py makemigrations

 migrate:
  - cd dapp && python manage.py migrate 

 shell:
  - cd dapp && python manage.py shell
  